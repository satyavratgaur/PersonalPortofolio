import React from 'react';
import styled from 'styled-components';

const SidebarContainer = styled.div`
width : 20%;
border : 10px solid red;
`

const Sidebar = () => {
  return <SidebarContainer>hi</SidebarContainer>;
};

export default Sidebar;
